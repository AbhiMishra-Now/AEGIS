# =============================================================================
# AEGIS Sentinel — Backend dependencies
# =============================================================================
# Python 3.11+
#
# Install:
#     pip install -r requirements.txt
#
# Run:
#     uvicorn main:app --reload --port 8000
# =============================================================================

# --- Web framework -----------------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.32.0
websockets==13.1
python-multipart==0.0.12

# --- Validation / config -----------------------------------------------------
pydantic==2.9.2
pydantic-settings==2.5.2

# --- HTTP client (used by the MCP poller to call Phoenix REST) ---------------
httpx==0.27.2

# --- Google Cloud ------------------------------------------------------------
# Vertex AI Agent Builder + the SDK we use to invoke + patch agents.
google-cloud-aiplatform==1.70.0
google-auth==2.35.0

# --- Gemini (LLM-as-judge) ---------------------------------------------------
# Official Gemini SDK. Reads GEMINI_API_KEY / GOOGLE_API_KEY.
google-genai==0.3.0

# --- Arize Phoenix + OpenInference -------------------------------------------
# Phoenix is the trace store; OpenInference is the instrumentation layer that
# sends spans from our GCP/Vertex AI calls into Phoenix automatically.
arize-phoenix==7.1.0
phoenix-mcp==0.1.0              # @arizeai/phoenix-mcp (Python wrapper)
openinference-instrumentation-vertexai==0.1.10
openinference-instrumentation-google-genai==0.1.10
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-exporter-otlp-proto-http==1.27.0

# --- Observability of the backend itself (optional but useful) ---------------
structlog==24.4.0
